import React from 'react';

const { Provider: TicketStoreServiceProvider, Consumer: TicketStoreServiceConsumer } = React.createContext();

export { TicketStoreServiceProvider, TicketStoreServiceConsumer };
